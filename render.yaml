services:
  - type: web
    name: roadeye-lpr
    env: python
    rootDir: backend

    pythonVersion: 3.10.13   # 👈 THIS LINE

    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt

    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
